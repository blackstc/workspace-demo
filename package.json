{
  "name": "workspace-demo",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "scripts": {
    "build:workspace-a": "cd ./packages/workspace-a && yarn build",
    "deploy:workspace-a": "cd ./packages/workspace-a && yarn deploy"
  },
  "devDependencies": {
    "firebase-tools": "^3.17.4",
    "gh-pages": "^1.1.0"
  }
}
